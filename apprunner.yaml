version: 1.0
build:
  commands:
    build:
      - npm ci --include=dev
      - npx prisma generate
      - npm run build
run:
  command: node dist/main
  network:
    port: 8080
    env: APP_PORT
  env:
    - name: NODE_ENV
      value: production
